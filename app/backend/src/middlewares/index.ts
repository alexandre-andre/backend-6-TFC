import handleError from './handle-error';
import isValidLogin from './valid-login-middleware';
import tokenAuthentication from './token-authetication-middleware';

export default {
  handleError,
  isValidLogin,
  tokenAuthentication,
};
